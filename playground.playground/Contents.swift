import UIKit

let constante = "Steve"
var variavel: String? = "Jobs"
var resultado: String?
if let variavel {
    print(constante, variavel, separator: " ")
} else {
    print(constante, "Wozniak", separator: " ")
}
